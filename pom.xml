<project
        xmlns="http://maven.apache.org/POM/4.0.0"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">


    <modelVersion>4.0.0</modelVersion>
    <groupId>com.arb</groupId>
    <artifactId>arb-bom</artifactId>
    <packaging>pom</packaging>
    <version>${revision}</version>
    <name>Alrajhi Bill of Materials project (ARB BOM)</name>


    <properties>
        <revision>0.0.1-SNAPSHOT</revision>
        <jasper-version>6.21.0</jasper-version>
        <prometheus-version>1.14.5</prometheus-version>
        <mysql-version>8.2.0</mysql-version>
        <webflux-ui-version>2.8.5</webflux-ui-version>
        <webmvc-ui-version>2.0.2</webmvc-ui-version>
        <map-struct-version>1.5.5.Final</map-struct-version>
        <lombok.version>1.18.30</lombok.version>
        <openpdf-version>1.3.30</openpdf-version>
        <spring-boot-security-version>3.4.5</spring-boot-security-version>
        <maven-deploy-version>3.1.0</maven-deploy-version>
        <spring.version>3.4.5</spring.version>
        <spring-web.version>6.2.6</spring-web.version>
        <jaxws-rt.version>2.3.3</jaxws-rt.version>
        <jakarta.xml.ws-api.version>3.0.1</jakarta.xml.ws-api.version>
        <javax.jws-api.version>1.1</javax.jws-api.version>
        <jakarta.jws-api.version>3.0.0</jakarta.jws-api.version>
        <webservices-rt.version>2.4.4</webservices-rt.version>
        <webservices-api.version>2.4.4</webservices-api.version>
        <javax.xml.soap-api.version>1.4.0</javax.xml.soap-api.version>
        <jaxws-api-version>2.3.1</jaxws-api-version>
        <saaj-impl.version>1.5.1</saaj-impl.version>
        <build-helper-maven-plugin.version>3.2.0</build-helper-maven-plugin.version>
        <jaxws-maven-plugin.version>2.6</jaxws-maven-plugin.version>
    </properties>


    <dependencyManagement>
        <dependencies>

            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-devtools</artifactId>
                <scope>runtime</scope>
                <optional>true</optional>
            </dependency>

            <dependency>
                <groupId>com.h2database</groupId>
                <artifactId>h2</artifactId>
                <scope>runtime</scope>
            </dependency>

            <dependency>
                <groupId>org.mapstruct</groupId>
                <artifactId>mapstruct</artifactId>
                <version>${map-struct-version}</version>
            </dependency>

            <dependency>
                <groupId>org.mapstruct</groupId>
                <artifactId>mapstruct-processor</artifactId>
                <version>${map-struct-version}</version>
                <scope>provided</scope>
            </dependency>

            <dependency>
                <groupId>org.projectlombok</groupId>
                <artifactId>lombok</artifactId>
                <version>${lombok-version}</version>
                <scope>provided</scope>
            </dependency>

            <dependency>
                <groupId>com.arb</groupId>
                <artifactId>arb-common-lib</artifactId>
                <version>${revision}</version>
            </dependency>

            <dependency>
                <groupId>com.arb</groupId>
                <artifactId>arb-soap-lib</artifactId>
                <version>${revision}</version>
            </dependency>

            <dependency>
                <groupId>net.sf.jasperreports</groupId>
                <artifactId>jasperreports</artifactId>
                <version>${jasper-version}</version>
            </dependency>

            <!-- JasperReports Java XML support -->
            <dependency>
                <groupId>net.sf.jasperreports</groupId>
                <artifactId>jasperreports-functions</artifactId>
                <version>${jasper-version}</version>

            </dependency>

            <!-- Font extension (optional, useful for PDF reports) -->
            <dependency>
                <groupId>net.sf.jasperreports</groupId>
                <artifactId>jasperreports-fonts</artifactId>
                <version>${jasper-version}</version>
            </dependency>

            <!-- JavaBeans DataSource -->
            <dependency>
                <groupId>net.sf.jasperreports</groupId>
                <artifactId>jasperreports-javaflow</artifactId>
                <version>${jasper-version}</version>
            </dependency>

            <!-- To enable swagger -->
            <dependency>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
                <version>${webmvc-ui-version}</version>
            </dependency>

            <!-- To enable swagger -->
            <dependency>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-starter-webflux-ui</artifactId>
                <version>${webflux-ui-version}</version>
            </dependency>


            <!-- https://mvnrepository.com/artifact/io.micrometer/micrometer-registry-prometheus -->
            <dependency>
                <groupId>io.micrometer</groupId>
                <artifactId>micrometer-registry-prometheus</artifactId>
                <version>${prometheus-version}</version>
            </dependency>

            <dependency>
                <groupId>com.mysql</groupId>
                <artifactId>mysql-connector-j</artifactId>
                <version>${mysql-version}</version>
            </dependency>

            <dependency>
                <groupId>com.github.librepdf</groupId>
                <artifactId>openpdf</artifactId>
                <version>${openpdf-version}</version>
            </dependency>

            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-starter-security</artifactId>
                <version>${spring-boot-security-version}</version> <!-- or latest available -->
            </dependency>

            <dependency>
                <groupId>org.springframework</groupId>
                <artifactId>spring-web</artifactId>
                <version>${spring-web.version}</version>
            </dependency>

            <dependency>
                <groupId>javax.xml.ws</groupId>
                <artifactId>jaxws-api</artifactId>
                <version>${jaxws-api-version}</version>
            </dependency>

            <!-- we need it to run the wsdl file after generated -->

            <dependency>
                <groupId>jakarta.jws</groupId>
                <artifactId>jakarta.jws-api</artifactId>
                <version>${jakarta.jws-api.version}</version>
            </dependency>

            <!-- we need it to run the wsdl file after generated -->

            <dependency>
                <groupId>javax.jws</groupId>
                <artifactId>javax.jws-api</artifactId>
                <version>${javax.jws-api.version}</version>
            </dependency>

            <!-- we need it to run the wsdl file after generated -->
            <dependency>
                <groupId>jakarta.xml.ws</groupId>
                <artifactId>jakarta.xml.ws-api</artifactId>
                <version>${jakarta.xml.ws-api.version}</version>
            </dependency>

            <!-- we need it to run the wsdl file after generated -->
            <!-- JAX-WS Runtime (needed if you call the service at runtime) -->
            <dependency>
                <groupId>com.sun.xml.ws</groupId>
                <artifactId>jaxws-rt</artifactId>
                <version>${jaxws-rt.version}</version>
            </dependency>

            <dependency>
                <groupId>org.glassfish.metro</groupId>
                <artifactId>webservices-rt</artifactId>
                <version>${webservices-rt.version}</version>
            </dependency>

            <dependency>
                <groupId>org.glassfish.metro</groupId>
                <artifactId>webservices-api</artifactId>
                <version>${webservices-api.version}</version>
            </dependency>

            <dependency>
                <groupId>javax.xml.soap</groupId>
                <artifactId>javax.xml.soap-api</artifactId>
                <version>${javax.xml.soap-api.version}</version>
            </dependency>

            <dependency>
                <groupId>com.sun.xml.messaging.saaj</groupId>
                <artifactId>saaj-impl</artifactId>
                <version>${saaj-impl.version}</version>
            </dependency>

        </dependencies>
    </dependencyManagement>

    <repositories>
        <repository>
            <id>spring-milestones</id>
            <name>Spring Milestones</name>
            <url>https://repo.spring.io/milestone</url>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
        </repository>
    </repositories>


    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.codehaus.mojo</groupId>
                    <artifactId>build-helper-maven-plugin</artifactId>
                    <version>${build-helper-maven-plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.codehaus.mojo</groupId>
                    <artifactId>jaxws-maven-plugin</artifactId>
                    <version>${jaxws-maven-plugin.version}</version>
                </plugin>
            </plugins>

        </pluginManagement>

        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-deploy-plugin</artifactId>
                <version>${maven-deploy-version}</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>deploy</goal>
                        </goals>
                        <phase>deploy</phase>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>


    <!-- to deploy on the specific repo -->
    <distributionManagement>
        <repository>
            <id>nexus-releases</id>
            <url>http://165.22.21.12:31983/:8081/repository/maven-releases/</url> <!-- URL for release repository -->
        </repository>
        <snapshotRepository>
            <id>nexus-snapshots</id>
            <url>http://165.22.21.12:31983/repository/maven-snapshots/</url> <!-- URL for snapshot repository -->
        </snapshotRepository>
    </distributionManagement>
</project>